name: Update request

on:
  schedule:
    - cron: "0 */3 * * *"
  workflow_dispatch:

jobs:
  send_request:
    runs-on: ubuntu-latest

    steps:
      - name: Send GET request
        run: curl -X GET "https://jellyfish-qw8x.onrender.com/update"
