name: Update request

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  send_request:
    runs-on: ubuntu-latest

    steps:
      - name: Send GET request
        run: curl -X GET "https://jellyfish-10r6.onrender.com/update"
